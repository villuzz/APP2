<core:FragmentDefinition xmlns="sap.m" xmlns:core="sap.ui.core">
	<SelectDialog 
  fieldGroupIds="progrGroup"
		id="ProgressHelp"
		title="{i18n>PROGRES}"
		items="{sProgress>/}"
		search=".ProgressSearch"
		confirm=".ProgressClose"
		cancel=".ProgressClose">
		<StandardListItem id="progrGroup" title="{sProgress>Progres}" description="{sProgress>Txt}" />
	</SelectDialog>
</core:FragmentDefinition>